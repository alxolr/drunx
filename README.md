# drunx 1.7.1

```
drunx 1.7.0
Productivity commands

USAGE:
    drunx <SUBCOMMAND>

FLAGS:
    -h, --help       Prints help information
    -V, --version    Prints version information

SUBCOMMANDS:
    aws        Aws Cli Shortcuts
    git        Git Cli helpers
    help       Prints this message or the help of the given subcommand(s)
    patch      Change the package.json, package-lock.json with the needed patch version
    version    Release the specified version in package.json, package-lock.json, README.txt
               adds a release commit and an annotated tag
```
